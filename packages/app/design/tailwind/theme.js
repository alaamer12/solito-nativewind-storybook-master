// @ts-check

/** @type {import('tailwindcss').Config['theme']} */
const theme = {
  colors: {
    grey: {
      light: '#E2E8F0',
      DEFAULT: '#bbc9dc',
      dark: '#bbc9dc',
    }
  },
  fontFamily: {
    sans: ['Graphik', 'sans-serif'],
    serif: ['Merriweather', 'serif'],
  },
}

module.exports = {
  theme,
}
